# 🧬 ML Medicine — Heart Disease Prediction

Этот репозиторий содержит модель машинного обучения для предсказания риска **сердечно-сосудистых заболеваний** на основе лабораторных и клинических данных.

Модель обучена на признаках:

`Gender, Age, BMI, Chol, HDL, LDL, Cr, BUN, TG`

и показывает **Accuracy ≈ 0.998** (по результатам экспериментов в ноутбуке Kaggle).

---

# 🚀 Использование (Inference)

## Требования
- Python 3.8+
- Установленные библиотеки:
  - pandas  
  - numpy  
  - scikit-learn  
  - joblib  

> Модель основана на sklearn (Logistic Regression / Random Forest).

---

# ❤️ Heart Disease Model (`heart_disease_model.joblib`)

**Тип задачи:**  
Binary Classification  
- `0` — низкий риск  
- `1` — высокий риск

**Формат входных данных:** DataFrame или словарь, преобразованный в DataFrame.

---

# 📥 Входные данные (Raw Input)

| Признак | Описание |
|--------|----------|
| Age | Возраст (лет) |
| Gender | Пол (`M` или `F`) |
| BMI | Индекс массы тела |
| Chol | Общий холестерин |
| TG | Триглицериды |
| HDL | Липопротеины высокой плотности |
| LDL | Липопротеины низкой плотности |
| Cr | Креатинин |
| BUN | Мочевина |
| Diagnosis | Целевой признак (только для обучения, не подавать в модель) |

---

# ⚙️ Препроцессинг (Обязательно)

Препроцессинг должен полностью совпадать с тем, что применялся при обучении.

## 1. Кодирование пола

### Вариант A — One-Hot Encoding (как в ноутбуке Kaggle)
```python
df = pd.get_dummies(df, columns=['Gender'])
```
### Вариант B — Бинарное кодирование
```python
df['Gender'] = df['Gender'].replace({'M': 0, 'F': 1, 'f': 1})
```
### Удаление лишних колонок
```python
if 'Unnamed: 0' in df.columns:
    df = df.drop(columns=['Unnamed: 0'])
```
### Предсказание
```python
import joblib
import pandas as pd

model = joblib.load("heart_disease_model.joblib")

prediction = model.predict(df[cols])        # 0 или 1
proba = model.predict_proba(df[cols])[:, 1] # вероятность
```
📊 Интерпретация результатов

prediction = 1 → высокий риск

prediction = 0 → низкий риск

### 📦 Установка
```
git clone https://github.com/AntonMininVIT/health-test-by-blood-dataset.git
```
